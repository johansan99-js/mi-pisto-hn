{
  "name": "mi-pisto-hn",
  "private": true,
  "description": "Pruebas automáticas de Mi Pisto HN (la app no tiene build: es index.html + sw.js)",
  "scripts": {
    "test": "node --test --test-concurrency=2 \"tests/*.test.js\""
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
